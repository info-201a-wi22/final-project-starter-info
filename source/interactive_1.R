# let's compare regular count of covid cases &/or mortality rates here, 
# maybe plot a line chart with population, deaths, & new covid cases in US?

data_int1 <- read_excel("../data/") %>% data.frame()
View(data_int1)
